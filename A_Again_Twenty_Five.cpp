#include<bits/stdc++.h>
using namespace std;

//#define long long int
#define all(x) (x).begin(), (x).end()

int lst2(long long int num){
    int one=num%10;
    num/=10;
    int two=num%10;
    two*=10;
    return (two+one);
}

int main(){
ios_base::sync_with_stdio(false);
cin.tie(NULL);
cout.tie(NULL);

    long long int n;
    cin>>n;
    long long int num;
    
    cout<<25;
    return 0;
}